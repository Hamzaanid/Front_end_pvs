import shop from '../../api/api_plaintes'
//import nested from './nested'

// initial state

const state = () => ({
  test:"jhhhh"
})

// getters
const getters = {
  
  }


// actions
const actions = {
  
}

// mutations
const mutations = {
  
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}